{
    "name": "Ashlyn",
    "description": "A multi-purpose Discord Bot.",
    "repository": "https://github.com/Spencer-0003/Ashlyn",
    "env": {
        "token": {
            "description": "Your discord bot's token.",
            "required": true
        },

        "google_api_key": {
            "description": "The API key for the music bot.",
            "required": true
        },

        "mongo_url": {
            "description": "The URL to the mongo datbase.",
            "required": true
        },

        "mongo_db": {
            "description": "The name of the database of itself.",
            "required": true
        },

        "error_log_channel": {
            "description": "Channel for error logs.",
            "required": true
        }
    }
}